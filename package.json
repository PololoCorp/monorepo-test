{
  "name": "monorepo-test",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "lint:ci": "turbo run lint:ci"
  },
  "devDependencies": {
    "turbo": "^1.2.16"
  }
}
